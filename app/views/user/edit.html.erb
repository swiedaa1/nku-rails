<h1>Edit post</h1>

<%= form_for :user, url: post_path(@post), method: :patch do |s| %>
  <% if @post.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= s.label :name %><br>
    <%= s.text_field :name %>
  </p>
 
  <p>
    <%= s.label :nickname %><br>
    <%= s.text_area :nickname %>
  </p>
 
  <p>
    <%= s.submit %>
  </p>
<% end %> 
 
<%= link_to 'Back', user_path %>
